<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/videoRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Video surface -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/playerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:use_controller="false"
        app:surface_type="surface_view" />

    <!-- Full-screen tap interceptor (toggle chrome / play-pause handled in code) -->
    <View
        android:id="@+id/tapInterceptor"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"
        android:clickable="true"
        android:focusable="true" />

    <!-- Center play / pause button -->
    <ImageButton
        android:id="@+id/btnPlayPause"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_gravity="center"
        android:background="@null"
        android:src="@drawable/ic_play_large"
        android:scaleType="fitCenter"
        android:contentDescription="@null" />

    <!-- Seek thumbnail preview — shown above scrubber while dragging -->
    <!-- translationX is set programmatically to track the thumb -->
    <FrameLayout
        android:id="@+id/seekThumbnailContainer"
        android:layout_width="120dp"
        android:layout_height="68dp"
        android:layout_gravity="bottom|start"
        android:layout_marginBottom="56dp"
        android:background="@drawable/bg_seek_thumb"
        android:clipToOutline="true"
        android:visibility="gone">

        <ImageView
            android:id="@+id/ivSeekThumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@null" />

    </FrameLayout>

    <!-- Bottom video controls (scrubber + mute) -->
    <LinearLayout
        android:id="@+id/videoControls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@drawable/bg_chrome_bottom"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingTop="24dp"
        android:paddingBottom="12dp"
        android:paddingStart="8dp"
        android:paddingEnd="4dp">

        <!-- Current position -->
        <TextView
            android:id="@+id/tvPosition"
            android:layout_width="44dp"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:textColor="@color/white"
            android:textSize="11sp"
            android:text="0:00" />

        <!-- Scrubber -->
        <SeekBar
            android:id="@+id/seekBar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginHorizontal="6dp"
            android:max="1000"
            android:thumbTint="@color/white"
            android:progressTint="@color/white"
            android:progressBackgroundTint="#80FFFFFF" />

        <!-- Total duration -->
        <TextView
            android:id="@+id/tvDuration"
            android:layout_width="44dp"
            android:layout_height="wrap_content"
            android:gravity="start"
            android:textColor="@color/white"
            android:textSize="11sp"
            android:text="0:00" />

        <!-- Mute / unmute -->
        <ImageButton
            android:id="@+id/btnMute"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_volume_on"
            android:contentDescription="@null" />

    </LinearLayout>

</FrameLayout>
